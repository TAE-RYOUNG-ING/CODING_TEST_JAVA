


// 풀이시간 00:12:10 & 제출점수 100점



class Solution {
  public int solution(int[] a, int[] b) {

    int sum = 0;

    for(int i=0; i<a.length; i++) {
      sum += a[i]*b[i];
    }

    return sum;
  }
}



// Stream 사용해서 푸는 방법
// 그러나 실행속도는 for문이 훨씬 빠름
// return IntStream.range(0, a.length).map(index -> a[index] * b[index]).sum();
